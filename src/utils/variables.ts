import { em } from "@mantine/core";

export const mobileWidth = `(max-width: ${em(767)})`;
export const tabletWidth = `(min-width: ${em(768)}) and (max-width: ${em(1023)})`;
export const desktopWidth = `(min-width: ${em(1024)})`;